# This file is managed by Puppet do not modify it from <%= fqdn %>
# My.cnf for MySQL 5.6 for Standalone default config
# filename=<%= file %>

[mysqld]
datadir=<%= mysqldatadir %>
server-id = <%= mysqld_serverid %>
report-host = <%= fqdn %>

# Error log
log-error=/sql/log/error.log

# Slow query log
log_output=FILE
slow_query_log=1
slow_query_log_file=/sql/log/slow-queries.log
long_query_time=5

# Binlog format
log-bin = /sql/binlog/bin.log
log-bin-index = /sql/binlog/bin.index
expire_logs_days = 7

# Connection params
max_connections=500
thread_cache_size=80
connect_timeout=20

# Site Setted to UTF8
character_set_server=utf8
character_set_filesystem=utf8
collation_server=utf8_general_ci
skip_character_set_client_handshake

# MyISAM Params
key_buffer_size=64M

# Innodb Params
innodb_buffer_pool_size=12G
innodb_log_file_size=512M
innodb_flush_method=O_DIRECT
innodb_stats_on_metadata=OFF

# Turn off the nonstandard behaviors for  TIMESTAMP
explicit-defaults-for-timestamp

# Keep server warm 
innodb_buffer_pool_dump_at_shutdown=ON
innodb_buffer_pool_load_at_startup=ON
innodb_buffer_pool_filename=ib_buffer_pool

# Undo tablespace
innodb_undo_directory=<%= mysqldatadir %>
innodb_undo_tablespaces=10
innodb_undo_logs=5

# GTID - needs more research
# gtid-mode = ON
# enforce-gtid-consistency = true

# From Zerolag * Fine Tuning section*
max_allowed_packet = 32M
max_heap_table_size = 128M
tmp_table_size = 128M

#key_buffer		= 1024M
#thread_stack		= 256K
#thread_cache_size	= 128
#table_cache            = 10000
#thread_concurrency     = 24
#read_buffer_size = 256k
#open_files_limit = 5000
#join_buffer_size = 16M
#low_priority_updates = 1
#sort_buffer_size	= 4M

# From Zerolag * Query Cache section *
query_cache_type = 1  #Added this because new default in 5.6 is query_cache_type=0

#query_cache_limit       = 8M
#query_cache_size        = 512M
#query_cache_min_res_unit = 1K

