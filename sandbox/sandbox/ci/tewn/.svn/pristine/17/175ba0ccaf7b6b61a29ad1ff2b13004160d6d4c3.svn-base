<?php

namespace CI\Filter;

class EmptyArray implements \CI\Filter
{

    public function filter($array)
    {
        foreach ($array as $key => $value) {
            if (is_null($value)) {
                unset($array[$key]);
            }
        }

        return $array;
    }
}