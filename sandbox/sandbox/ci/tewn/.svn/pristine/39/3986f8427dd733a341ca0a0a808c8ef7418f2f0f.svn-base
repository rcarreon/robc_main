<? 
	if (!isset($canonical)) {
        $canonical = "";
    }

	echo $this->element(DOMAIN."/header", array("title_for_layout" => $title_for_layout, "canonical" => $canonical));

	flush();

	echo $content_for_layout;

	echo $this->element(DOMAIN."/footer");
?>
