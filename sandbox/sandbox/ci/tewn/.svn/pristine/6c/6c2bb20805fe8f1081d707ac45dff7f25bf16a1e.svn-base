<?
	if (!isset($canonical)) {
        $canonical = "";
    }

	echo $this->element("header", array("title_for_layout" => $title_for_layout, "canonical" => $canonical));

	echo $this->element("toolbar");

	flush();

	echo $content_for_layout;

//	echo $this->element("right_sidebar");

	echo $this->element("footer");
?>
