<?php echo $html->css('settings'); ?>

<div class="row main">
	<div id="content" class="column grid_12">
		<div class="column grid_12">

			<?php echo $this->element("management_toolbar", array("tab" => "settingstoolbar","user" => $user));?>

			<h3 class="topContent">Tunables</h3>

			<div class="row" id="content_area">
				<div id="settings">
					<form method="post" action="/settings/hub/<?php echo $hub['id']?>">

						<?php
							$session->activate();
							if ($session->check('Message.flash')) {
								$session->flash();
							}
						?>

						<h3>Tunables for <?php echo $hub['name']; ?></h3>

						<p>Do we penalize low credit accounts<br />
						<br />
						Only need to do this if we have a few accounts we<br />
						can't keep up with<br /></p>

						<label for="widget_penalize_input">widget_penalize</label>: <input type="text" value="<?php echo isset($settings['Setting']['widget_penalize_hub_'.$hub['id']]) ? $settings['Setting']['widget_penalize_hub_'.$hub['id']] : 0 ; ?>" name="Setting[widget_penalize]" id="widget_penalize_input" /><br />

						<p>At what point do we penalize<br />
						<br />
						Number of credits that if under we penalize <br />
						<br />
						database form</p>
						<label for="widget_penalize_threshold_input">widget_penalize_threshold</label>: <input type="text" value="<?php echo isset($settings['Setting']['widget_penalize_threshold_hub_'.$hub['id']]) ? $settings['Setting']['widget_penalize_threshold_hub_'.$hub['id']] : 200 ; ?>" name="Setting[widget_penalize_threshold]" id="widget_penalize_threshold_input" /><br />

						<p>Do we penalize small sites<br />
						<br />
						Do this if we have small sites that are getting way too much traffic<br /></p>
						<label for="widget_penalize_small_site_input">widget_penalize_small_site</label>: <input type="text" value="<?php echo isset($settings['Setting']['widget_penalize_small_site_hub_'.$hub['id']]) ? $settings['Setting']['widget_penalize_small_site_hub_'.$hub['id']] : 1 ; ?>" name="Setting[widget_penalize_small_site]" id="widget_penalize_small_site_input" /><br />

						<p>At what point do we penalize small sites<br />
						<br />
						Amount of incoming traffic</p>
						<label for="widget_penalize_small_site_threshold_input">widget_penalize_small_site_threshold</label>: <input type="text" value="<?php echo isset($settings['Setting']['widget_penalize_small_site_threshold_hub_'.$hub['id']]) ? $settings['Setting']['widget_penalize_small_site_threshold_hub_'.$hub['id']] : 20 ; ?>" name="Setting[widget_penalize_small_site_threshold]" id="widget_penalize_small_site_threshold_input" /><br />

						<p>Do we penalize low credit accounts<br />
						<br />
						Only need to do this if we have a few accounts we<br />
						can't keep up with</p>
						<label for="landing_page_penalize_input">landing_page_penalize</label>: <input type="text" value="<?php echo isset($settings['Setting']['landing_page_penalize_hub_'.$hub['id']]) ? $settings['Setting']['landing_page_penalize_hub_'.$hub['id']] : 1 ; ?>" name="Setting[landing_page_penalize]" id="landing_page_penalize_input" /><br />

						<p>At what point do we penalize<br />
						<br />
						Number of credits that if under we penalize<br />
						<br />
						database form</p>
						<label for="landing_page_penalize_threshold_input">landing_page_penalize_threshold</label>: <input type="text" value="<?php echo isset($settings['Setting']['landing_page_penalize_threshold_hub_'.$hub['id']]) ? $settings['Setting']['landing_page_penalize_threshold_hub_'.$hub['id']] : 200 ; ?>" name="Setting[landing_page_penalize_threshold]" id="landing_page_penalize_threshold_input" /><br />

						<p>Do we penalize small sites<br />
						<br />
						Use this if have small sites getting way too much traffic</p>
						<label for="landing_page_penalize_small_site_input">landing_page_penalize_small_site</label>: <input type="text" value="<?php echo isset($settings['Setting']['landing_page_penalize_small_site_hub_'.$hub['id']]) ? $settings['Setting']['landing_page_penalize_small_site_hub_'.$hub['id']] : 1 ; ?>" name="Setting[landing_page_penalize_small_site]" id="landing_page_penalize_small_site_input" /><br />

						<p>At what point do we penalize small sites<br />
						<br />
						Amount of incoming traffic<br /></p>
						<label for="landing_page_penalize_small_site_threshold_input">landing_page_penalize_small_site_threshold</label>: <input type="text" value="<?php echo isset($settings['Setting']['landing_page_penalize_small_site_threshold_hub_'.$hub['id']]) ? $settings['Setting']['landing_page_penalize_small_site_threshold_hub_'.$hub['id']] : 200 ; ?>" name="Setting[landing_page_penalize_small_site_threshold]" id="landing_page_penalize_small_site_threshold_input" /><br />

						<p>Ideal return rate for websites<br />
						</p>
						<label for="ideal_return_rate_input">ideal_return_rate</label>: <input type="text" value="<?php echo isset($settings['Setting']['ideal_return_rate_hub_'.$hub['id']]) ? $settings['Setting']['ideal_return_rate_hub_'.$hub['id']] : 150 ; ?>" name="Setting[ideal_return_rate]" id="ideal_return_rate_input" /><br />

						<h3>CPC tunables for <?php echo ucfirst($hub['name']); ?></h3>

						<p>Percentage of paid links on landing page<br />
						<br />
						Use this value to increase the number of paid links on the landing page, the value is given as a percentage from 0 to 100.<br /></p>
						<label for="cpc_landing_page_percentage_of_paid_links">cpc_landing_page_percentage_of_paid_links</label>: <input type="text" value="<?php echo isset($settings['Setting']['cpc_landing_page_percentage_of_paid_links_hub_'.$hub['id']]) ? $settings['Setting']['cpc_landing_page_percentage_of_paid_links_hub_'.$hub['id']] : 0 ; ?>" name="Setting[cpc_landing_page_percentage_of_paid_links]" id="cpc_landing_page_percentage_of_paid_links" /><br />
						<input type="submit" value="Save" />

					</form>
				</div>
			</div>
		</div>
	</div>
</div>





