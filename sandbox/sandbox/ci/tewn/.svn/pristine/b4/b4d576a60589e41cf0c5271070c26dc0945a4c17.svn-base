<span class="title">Use existing page</span>
<div class="content">
    <?php
    $options = array(
        'update' => 'use-existing',
        'url'    => '/cpc/pages_by_website',
        'complete' => "jQuery('#auto_complete_loading').hide();jQuery('#auto_complete').show();on_results_complete('use-existing-page');",
    );

    echo $ajax->form(
        array(
            'type' => 'get',
            'options' => $options,
        )
    );

    ?>
        <input type="text" name="search" class="large">
        <label>
            Limit:
            <input type="text" name="limit" class="small" value="10">
        </label>
        <input id="use-existing-page" name="page" type="hidden" value="1" />
        <input name="website_id" type="hidden" value="<?php echo $website_id; ?>" />
        <input id="btn-use-existing-search" class="btn" type="submit" value="Search" />
    </form>
    <div class="table_container">
        <div id="use-existing-loading" class="preloaderbox hide"></div>
        <div id="use-existing" class="use-existing">
            <!-- Results will load here -->
        </div>
        <div class="options">
            <a href="#" class="btn save">Submit</a>
            <a href="#" id="btnCloseModal" class="btn exit">Close</a>
        </div>
    </div>
</div>