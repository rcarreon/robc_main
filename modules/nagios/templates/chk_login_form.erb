# $Id$
# $URL$

define service {
        host_name                      <%= dom %>
        service_description            <%= name %>
        check_command                  check_form!<%= full_url %>!<%= auth %>!<%= str %>
        notification_options           w,u,c,r,f,s
        use                            generic-service
        notes_url                       http://docs.gnmedia.net/wiki/Nagios-check_form
}

