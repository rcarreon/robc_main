#check uri by regex
define service {
        host_name                      <%= dom %>
        service_description            <%= name %>
        check_command                  check_json.pl!<%= url %>
        notification_options           w,u,c,r,f,s
        use                            generic-service
        notes_url                      http://docs.gnmedia.net/wiki/Nagios-check_json
}

