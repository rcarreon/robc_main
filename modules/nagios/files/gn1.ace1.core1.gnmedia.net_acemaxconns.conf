$seekfilesdir = '/var/tmp/check-acemaxconns';
# where the state information will be saved.

# $protocolsdir = '/opt/nagios/var/tmp';
# where protocols with found patterns will be stored.

@searches = (
  {
    logfile => '/tmp/gn1.ace1.core1.gnmedia.net_acemaxconns.log',
    criticalpatterns => ['CRITICAL'],
    options => 'noprotocol',
  },
);
