# Credentials for si_build user, for use with scripting.
# can  be used like so:
# mysql -–defaults-extra-file=/etc/si_build/si_build.my.cnf --host=sql1v-dh.si.dev.lax.gnmedia.net somecommand
#

[client]

    user=si_build

    password=<%= sibuildpw %>


