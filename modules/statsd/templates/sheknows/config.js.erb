{
  "graphitePort": 2003
, "graphiteHost": "relay-graphite.gnmedia.net"
, "port": 8125
, "statPrefixes": {
<%
stats = ['average','counters','timers','gauges']
if (fqdn =~ /^(app[0-9]+v)-(\w*)\.(\w*)\.(\w*)/ )
  stats.each do |counter|
%>
    <%= counter != 'average' ? ', ' : '' %>"<%= counter %>": "statsd.<%= $3 %>.<%= $2 %>.<%= $4 %>.<%= $1 %>.<%= counter %>."
<%
  end
end
%>
  }
}
