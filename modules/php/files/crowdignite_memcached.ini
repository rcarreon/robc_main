; $Id$
; $URL$

; Enable memcached extension module
extension=memcached.so


; ----- Options to use the memcached session handler

;  Use memcache as a session handler
;session.save_handler=memcached
;  Defines a comma separated list of server urls to use for session storage
;session.save_path="localhost:11211"

; memcached doesn't like tcp://, and we can't add option like memcache just server and port, that's it.

; done on cake and crowdignite use memcache not memcached
;session.save_handler=memcached
;session.save_path="
;                       10.11.34.45:11211,
;                       10.11.34.46:11211,
;                       10.11.34.53:11211,
;                       10.11.34.107:11211,
;                       10.11.34.108:11211,
;         "
