# Configuration file for the Subversion service

# $Id$
# $URL$

#
# To pass additional options (for instace, -r root of directory to server) to
# the svnserve binary at startup, set OPTIONS here.
#
#OPTIONS=
OPTIONS="--threads --root /ops/repo/svn"
